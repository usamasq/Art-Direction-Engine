# Assets

Visual references for the repo. Two kinds live here:

1. **Diagrams** — the flow/dependency diagrams in the README and START_HERE are written as [Mermaid](https://mermaid.js.org/) and render directly on GitHub, so they need no image files.
2. **Example renders** — the files in [`assets/examples/`](examples/), rendered from the prompts in the [worked example](../examples/worked_reel_EmberAndComb.md), so readers can see the system's output, not just read about it.

> All example imagery should be from the **fictional** Ember & Comb brand (or another fictional brand). Keep real client renders out of the public repo.

## Example renders — Ember & Comb "The Drizzle" reel

Rendered from the prompts in the [worked example](../examples/worked_reel_EmberAndComb.md) and saved in this folder:

| File | What it shows | From |
|---|---|---|
| `ember_jar_packshot.jpeg` | The invented jar — the reference everything derives from | Step 0 |
| `ember_scene_ref.jpeg` | The empty dark-kitchen scene plate | Step 1a |
| `ember_jar_sheet.jpeg` | The jar multi-angle reference sheet | Step 1b |
| `ember_master_frame.jpeg` | The approved hero still (board + chicken + jar) | Step 2 |
| `ember_shot2_end.jpeg` | Dipper lifted, first amber ribbon falling | Step 3, Shot 2 |
| `ember_shot3_macro.jpeg` | Macro — honey landing, chili flecks | Step 3, Shot 3 |
| `ember_shot4_reach.jpeg` | Someone reaching in, honey thread | Step 3, Shot 4 |
| `ember_reel.mp4` | The assembled ~10s reel | Step 4–5 |

Reference them from the docs (paths relative to the repo root), e.g. `![Ember & Comb master frame](assets/examples/ember_master_frame.jpeg)`.
